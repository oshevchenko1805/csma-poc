> 🛑 **ЧИСЛА ДЕТЕКЦИИ УСТАРЕЛИ (4 августа).** Поле `detected` считалось
> без пост-атакового гейта и без проверки `target_uav`, поэтому
> передатаковые ложные срабатывания шли в зачёт. Верные значения:
> detector_takeout **0/28, 0/30**, 30/30; monitor_takeout **0/28**,
> 29/30, 30/30; gps_spoofing 14/15, 15/15, **14/15**. Механизм и
> последствия — `RESULTS_NOTES.md`, R18. Действующая таблица —
> `HANDOFF_CH45.md` §3.1.

# Выводы из результатов и план рисунков

Составлено заново из `runs_campaign/campaign_master.csv` (435 строк, 414
валидных, 20 errored, 1 отсеян гейтом, `post_fix` True) и
`runs_final/detection_vs_loss.csv`. Ничего не взято из прежних сводок без
пересчёта.

---

## 0. Почему семь рисунков не работали

Все три раунда организовывали материал **по метрикам**: рисунок на
обнаружение, рисунок на деградацию, рисунок на координацию. Каждый такой
рисунок — матрица 5 атак × 3 архитектуры одного скаляра. В этой матрице
11–12 ячеек из 15 совпадают. Столбик кодирует скаляр, поэтому одинаковые
ячейки дают одинаковые столбики, и рисунок честно показывает пустоту.

Но одинаковость ячеек — это **не шум, это и есть результат**. Данные
структурированы не по метрикам, а по тому, **какой слой системы
скомпрометировала атака**. Как только материал переразбить по этому
признаку, повторы превращаются в блоки, а блоки уже видно.

---

## 1. Главный вывод: четыре режима, а не пять атак

Эффективность самовосстановления ограничена тем, **уцелела ли та
информация, на которую опирается восстановление**. Это одно утверждение
покрывает все пятнадцать ячеек — включая те, где C проигрывает.

### Режим I — атака на саму плоскость безопасности
`detector_takeout+gps`, `monitor_takeout+gps`

Единственное место, где различается **обнаружение**:

| | detector_takeout | monitor_takeout |
|---|---|---|
| A | **0/28 (0.00)** | **0/28 (0.00)** |
| B | **0/30 (0.00)** | 29/30 (0.97) |
| C | 30/30 (1.00) | 30/30 (1.00) |

Здесь важна не бинарность «C лучше», а **лестница A < B < C**:
сегментация (B) спасает от потери *процесса* мониторинга, но не от
потери *логики* обнаружения. От второго спасает только перекрёстная
проверка с соседом. A не спасает ни от чего.

Во всех остальных сценариях обнаруживают все три: 14/15 … 15/15.

### Режим II — атака на командный канал
`command_injection`

Обнаруживают мгновенно все три (MTTD 0.01 с — это событие, а не оценка).
Разница целиком в **реакции**:

| | стабилизация | отклонение, м | фаза, м | геометрия, м |
|---|---|---|---|---|
| A | 0/15 | 37.5 | 154.1 | 57.0 |
| B | 0/15 | 37.6 | 151.9 | 59.2 |
| C | 14/15 (MTTR 0.12 с) | 0.41 | 0.2 | 0.07 |

Единственная клетка, где C выигрывает **по всем осям сразу**. Причина:
оценка состояния не тронута, поэтому реакция может отфильтровать
команду и *продолжить миссию*.

### Режим III — атака на саму оценку состояния
`gps_spoofing` и GPS-компонента takeout-сценариев

Обнаруживают все (когда детектор жив). Реакция принципиально
ограничена: доверенной позиции больше нет, остаётся loiter.

| | отклонение, м | фаза, м |
|---|---|---|
| A | 49.84 | 14.1 |
| B | 49.83 | 15.4 |
| C | 19.66 | 145.5 |

C **разменивает** позиционную ошибку на фазовую. A и B не разменивают
ничего: они сохраняют строй и доезжают спуф до конца.

**Оговорка, которую нельзя опустить:** 49.84 м у A и B — это не
измеренная деградация, а **величина инжектированного смещения** (потолок
≈ 50 м). MTTR у них 51–54 с — это момент насыщения рампы спуфа, а не
действие архитектуры. Число цензурировано и в таком виде на рисунок
выносить нельзя.

### Режим IV — атака вне плоскости безопасности (контроль)
`comm_disruption`

Все три идентичны по всем осям: MTTD 3.4 с, отклонение 1.1–1.7 м, фаза
107/111/117 м, геометрия ~36 м. Потеря канала роняет аппарат в failsafe
PX4 — это не эффект архитектуры безопасности и приписывать его ей
нельзя. Ценность строки — **отрицательный контроль**: она доказывает,
что различия в режимах I–III не есть общее смещение «C лучше».

---

## 2. Цена

- **Ложные срабатывания на чистых прогонах (attack == none, N=35/арх):**
  A 2/35, B 4/35, **C 0/35**. C не шумнее.
- **Пересчитать прежнюю формулировку.** Цифры «A 6/136, B 9/140, C
  2/138» смешивают чистые прогоны и предатаковые окна атакующих
  прогонов. Это разные популяции, одной «частотой ложных срабатываний»
  их называть нельзя.
- **Цена ложного срабатывания у C — в радиусе, не в частоте.** Всего два
  случая за кампанию, но по 8 и 5 событий (у A и B максимум 3), и один
  из них (`C_command_injection_r1_1785410951`) стоил 143.7 м фазового
  расхождения при норме 0.2 м. n=2 — это текст и таблица, не рисунок.
- **Стоимость меша:** 468 сообщений / 124.5 КБ / 936 доставок за прогон,
  0 потерь; у A и B ровно 0. Три столбика, два нуля — таблица.
- **MTTD:** 2.9–3.4 с у всех в режимах II–IV. Мешевый путь добавляет
  ~4.5 с (7.4 с). **Преимущества по скорости у C нет** — это надо
  сказать, а не рисовать.

---

## 3. Что из этого рисунок, а что таблица

| Утверждение | Природа данных | Вердикт |
|---|---|---|
| Лестница A<B<C по обнаружению | 15 ячеек, 4 информативных | рисунок **как карта режимов**, не как столбики |
| Размен «сдерживание ↔ координация» | двумерная структура, 12 плотных кластеров | **рисунок**, скаттер |
| Деградация детекции от потерь канала | кривая по параметру + CI | **рисунок**, бесспорно |
| Плато k = 2…4 | две кривые по параметру | **рисунок**, бесспорно |
| comm_disruption как контроль | 3 одинаковых числа | таблица |
| Стоимость меша | 3 числа, два нуля | таблица |
| Ложные срабатывания | 0/35, 2/35, 4/35 + два выброса | таблица + абзац |
| MTTD | плоско везде | таблица |

---

## 4. Предлагаемые четыре рисунка

### Рис. 1 (новый) — карта режимов, 5 × 3

Не скаляр в ячейке, а **класс исхода**, порядковый:

0 — не обнаружено · 1 — обнаружено, не сдержано · 2 — сдержано, координация
утрачена · 3 — восстановлено полностью

Заливка по классу, в ячейке — подтверждающие числа мелким шрифтом.

Почему это работает там, где столбики провалились: повторяющиеся ячейки
складываются в **блоки одного тона**, и рисунок показывает *узор*, а не
пятнадцать одинаковых высот. Видно сразу: строка `comm` одинакова
(контроль), строка `command_injection` — единственный полный успех C,
строки takeout — провал обнаружения у A и половинчатость у B, строка
`gps` — сдерживание ценой строя. Проигрыш C не прячется, а становится
отдельным классом.

### Рис. 2 (новый, заменяет забракованные fig3 и fig4) — размен

Скаттер: x = отклонение от миссии (сдерживание), y = фазовое расхождение
(цена для координации). Точка = прогон, форма = архитектура, подпись у
кластера = сценарий. Обе оси линейные, никаких логарифмов.

Что видно геометрически, чего таблица не покажет:

- A и B схлопываются в один плотный кластер у (49.8, 14) — правый нижний
  угол, и это **упор в потолок инжекции**, что надо подписать прямо;
- C при GPS уходит **влево и вверх**: (19.7, 145) и (0.2, 173) — размен
  виден как перемещение, а не как два столбика разного масштаба;
- C при `command_injection` сидит **в начале координат** (0.4, 0.2) —
  и сразу ясно, что это другой режим, а не «C вообще лучше».

Разбросы очень плотные (IQR по большинству кластеров единицы метров),
так что кластеры не сольются.

### Рис. 3 — свип по потерям канала

Существующий `fig7_losssweep`. Претензий не было, содержание бесспорно:
0.96 → 0.25 при потерях 0 → 0.65, плато 0.46, в ноль не падает; MTTD от
потерь не зависит (7.4–8.3 с). Оставить как есть.

### Рис. 4 — плато правила подтверждения k

Существующий `fig6_sustain`. Оставить как есть.

---

## 5. Что удалить

`fig1_detection`, `fig2_degradation`, `fig3_tradeoff`, `fig4_recovery`,
`fig5_falsepositive` — материал переезжает в таблицы и в рисунки 1–2.
`fig6` и `fig7` перенумеровать в 3 и 4.

Итого: **4 рисунка вместо 7**, и каждый показывает то, чего нет в
таблице.

---

## 6. Не проверено, требует подтверждения на VM

Отклонение у C в режиме III равно ≈ высоте рампы спуфа на момент
стабилизации: loiter при MTTD 2.9 с даёт 19.7 м, при 7.4 с — 0.24 м
(корреляция MTTD × отклонение внутри `gps_spoofing × C` равна **−0.98**).
Отсюда оценка длительности рампы ≈ 4.8 с. Это **вывод из чисел, а не
измерение** — проверяется по `trajectory.jsonl`, которого нет в
маковском клоне. Если подтвердится, это отдельный сильный абзац в главе
5 о том, что сдерживание у C пропорционально скорости реакции; если нет
— из текста убрать, на рисунки 1 и 2 это не влияет.
