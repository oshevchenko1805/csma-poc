# HANDOFF — главы 4 и 5

Единственный входной файл для нового чата по тексту глав 4 и 5. Всё, что
нужно, либо здесь, либо по указанным путям в этом репозитории.

Заменяет `HANDOFF_WRITING.md` и `HANDOFF_CH3_ALIGNMENT.md`. Старые
хендоффы читать не нужно; расхождения с ними перечислены в разделе 7.

**Рисунки готовы**, набор из четырёх закрыт. См. раздел 5 и
`FIGURE_SPECS.md`. `HANDOFF_FIGURES.md` устарел — это история отклонённых
попыток, для работы не нужна.

---

## Карта хендоффов

| файл | для чего | статус |
|---|---|---|
| **`HANDOFF_CH45.md`** (этот) | текст глав 4 и 5 | актуален |
| **`FIGURE_SPECS.md`** | рисунки: спецификация и обоснование каждого | **актуален**, набор закрыт |
| `HANDOFF_FIGURES.md` | история отклонённых попыток | **устарел**, для работы не нужен |
| `HANDOFF_WRITING.md` | предыдущая фаза | **устарел**, не читать |
| `HANDOFF_CH3_ALIGNMENT.md` | глава 3 | выполнен, содержит **неверное** указание про MTTR (см. 7.2) |
| `HANDOFF.md`, `HANDOFF_NEXT.md`, `HANDOFF_ANALYSIS.md` | фазы кампании и анализа | архив, ценны только как источник провенанса |

---

## 0. Правила работы

- Русский, коротко, по сути, один микро-шаг за раз.
- Не знаешь / не сработает / мало инфы — говори сразу, брутально честно.
  Костыль от чистой инженерии отделять явно.
- **Любое число в тексте приходит из `runs_campaign/campaign_master.csv`
  или `runs_final/detection_vs_loss.csv`.** Не из памяти, не из старых
  сообщений, не из старых секций `RESULTS_NOTES.md`.
- Текст диссертации на украинском. **Длинных тире не использовать** —
  в главах 2 и 3 их нет, вставка с ними выделяется.
- Объём блока: один підрозділ за сессию, 250–350 слов на смысловую
  единицу (замерено по существующим: injection cases 235–283 слова).
- К каждому блоку — манифест чисел с указанием источника.
- Прогонов НЕ нужно. Симуляция и анализ закрыты.

---

## 1. Где что лежит

| Что | Путь | Комментарий |
|---|---|---|
| **Мастер-файл, 1 строка = 1 прогон, 435 строк, 47 колонок** | `runs_campaign/campaign_master.csv` | единственный источник чисел кампании |
| **Свип по потерям меша, 10 уровней** | `runs_final/detection_vs_loss.csv` | финальный, belief-гейтованный, 255 валидных |
| Промежуточные срезы кампании | `runs_campaign/campaign.csv`, `coordination.csv`, `baseline_coord.csv` | производные, для диагностики |
| Рисунки, PNG 300 dpi + PDF вектор | `figures/fig4_1..fig4_4` | см. раздел 5 и `FIGURE_SPECS.md` |
| Все результаты с механизмами | `RESULTS_NOTES.md`, R1–R19 | читать целиком; R17–R19 добавлены 4 августа |
| Реестр раскрытия | конец `RESULTS_NOTES.md` | что пишем, чего не пишем |
| Сверка главы 3 с реализацией | `thesis_text/AUDIT_CH3_vs_IMPLEMENTATION.md` | |
| Незакрытые правки главы 3 | `thesis_text/INSERTS_CH3_ROUND2.md`, части B-1…B-5 | см. раздел 8 |
| Текст injection cases 4–5 | `thesis_text/3.5.3_injection_cases_4_5.md` | уже внесён в диссертацию |
| Определения метрик | `metrics/derived.py`, шапка файла | точные формулы |
| Статистика | `metrics/stats.py`, `campaign_stats.py` | Wilson, Fisher, Newcombe, Mann-Whitney, bootstrap |
| Построение рисунков | `metrics/figures_ch4.py` — единая точка входа | одна команда на весь набор |

Репозиторий `oshevchenko1805/csma-poc`, ветка `main`. На VM
`~/csma_poc_v2/`, на Маке клон в `~/Documents/csma/csma-poc`.
Синхрон: правка на Маке → `git push` → на VM `git pull`, и наоборот.

Сырые данные (6.3 ГБ) под `.gitignore` и для текста не нужны. Бэкап
существенного: `~/csma_data_20260731.tgz` (106 МБ, 921 прогон).
**Держать копию вне VM.**

---

## 2. Как подтвердить, что берёшь правильные числа

```bash
cd ~/csma_poc_v2 && source .venv/bin/activate
git log --oneline -1          # должен совпасть с клоном на Маке
python -m pytest -q           # ожидаем 816 passed
```

Проверка провенанса кампании одной командой:

```bash
python - <<'EOF'
import pandas as pd
d=pd.read_csv('runs_campaign/campaign_master.csv'); v=d[d.valid==True]
print('строк', len(d), '| валидных', len(v),
      '| errored', int(d.errored.sum()),
      '| отсеяно гейтом', len(d)-len(v)-int(d.errored.sum()))
print('все post_fix:', bool(d.post_fix.all()))
EOF
```

Ожидается: **435 / 414 / 20 / 1, post_fix True**. Если не сходится —
мастер-файл пересобран, и всё в тексте надо перепроверять.

Пересобрать мастер-файл (не нужно без причины):

```bash
ROOTS=$(echo runs_campaign/{std_pass1,ci_pass{1,2,3},dt_pass{1,2,3,4,5,6},\
mt_pass{1,2,3,4,5,6},gps_pass{1,2},comm_pass{1,2},base_pass{1,2,3,4,5,6}})
python campaign_master.py $ROOTS --csv runs_campaign/campaign_master.csv
```

Пересобрать рисунки:

```bash
python -m metrics.figures_ch4
```

Любое число из раздела 3 воспроизводится группировкой мастер-файла по
`architecture` × `attack` при `valid == True`. Шаблон:

```bash
python -c "
import pandas as pd
v=pd.read_csv('runs_campaign/campaign_master.csv').query('valid==True')
print(v.pivot_table(index='attack',columns='architecture',
      values='ПОЛЕ',aggfunc='median').round(2))"
```

---

## 3. Все валидные результаты

Кампания: 657 прогонов на диске → 435 в анализе → **414 валидных**
(20 упавших, 1 отсеян валидационным гейтом). Дофиксовых 0, проверяется
машинно по `POST_FIX_EPOCH = 1785265956`.

### 3.1 Detection rate, Wilson 95% CI

✅ **Пересчитано 4 августа после исправления гейта детекции.** Мастер-файл
пересобран (`03fd3d4`), провенанс тот же 435 / 414 / 20 / 1, post_fix
True, инвариант `detected ⇒ конечный MTTD` чист.

**Что было не так.** `metrics/derived.py` считал `detected` как
«существует любое security-событие в прогоне» — без временного гейта и
без проверки `target_uav`. Передатаковые ложные срабатывания шли в зачёт
как обнаружение атаки. Восемь прогонов из 228. Признак был виден в самом
мастер-файле: у всех восьми `detected == True` при пустом `mttd_s`,
потому что гейт был реализован у MTTD и не был у `detected`.

Пересборка изменила **ровно 8 строк в ровно одном поле** — сверено
по-строчно с предыдущей версией. Деградация, фаза, MTTR, стоимость меша,
FP не сдвинулись ни на одну строку.

| атака | A | B | C |
|---|---|---|---|
| detector_takeout+gps | **0/28 = 0.00 [0.00, 0.12]** | **0/30 = 0.00 [0.00, 0.11]** | **30/30 = 1.00 [0.89, 1.00]** |
| monitor_takeout+gps | **0/28 = 0.00 [0.00, 0.12]** | 29/30 = 0.97 [0.83, 0.99] | **30/30 = 1.00 [0.89, 1.00]** |
| gps_spoofing | 14/15 = 0.93 [0.70, 0.99] | 15/15 = 1.00 [0.80, 1.00] | **14/15 = 0.93 [0.70, 0.99]** |
| comm_disruption | 15/15 = 1.00 [0.80, 1.00] | 15/15 = 1.00 [0.80, 1.00] | 13/13 = 1.00 [0.77, 1.00] |
| command_injection | 15/15 = 1.00 [0.80, 1.00] | 15/15 = 1.00 [0.80, 1.00] | 15/15 = 1.00 [0.80, 1.00] |
| none | 0/35 | 0/35 | 0/35 |

Никогда не писать «100 %» без интервала. **То же касается нуля:** 0/28 —
это 0.00 [0.00, 0.12], а не «никогда».

**Три следствия для чтения.**

1. Главный результат усилился: A и B уходят в точный ноль под
   detector_takeout против 30/30 у C.
2. Правка не односторонняя — C теряет одно обнаружение под gps_spoofing.
   Согласуется с R14 (пост-атаковое окно 43/45 против полного 44/45).
3. **Под обычным GPS-спуфингом C теперь ровно равна A: 14/15 обе.**
   Раньше C выглядела 15/15 против 14/15, что читалось как слабое
   преимущество. Его нет. Это укрепляет тезис В1: преимущество C
   локализовано в атаках на плоскость безопасности, а не общее.

**Не затронуто, проверено:** свип по потерям
(`analyze_loss_sweep.py` считает `cross_check AFTER attack onset` и
ведёт `fp_preattack` отдельно), рис. 4.2 (пересчитывается из
`ratio_maxcons`, поле `detected` не трогает), рис. 4.3, рис. 4.4.

⚠️ **`campaign_stats.py` читает каталоги прогонов, а не мастер-файл.**
Это второй источник истины для тех же чисел в обход правила из §0.
Правку `derived.py` он подхватывает автоматически, потому что идёт через
`D.analyse_run()`, но расхождение с мастером он не обнаружит. Числа §3.2
ниже посчитаны из мастер-файла напрямую.

### 3.2 Первичное семейство сравнений (Холм по четырём, объявлено в коде до просмотра p)

Пересчитано 4 августа из мастер-файла напрямую (Fisher two-sided,
разность долей с интервалом Newcombe, Холм по четырём).

| сравнение | C | baseline | diff [Newcombe 95 %] | p_adj |
|---|---|---|---|---|
| detector_takeout, C vs A | 30/30 | 0/28 | **+1.00 [+0.83, +1.00]** | 1.0e-16 |
| detector_takeout, C vs B | 30/30 | 0/30 | **+1.00 [+0.84, +1.00]** | 6.8e-17 |
| monitor_takeout, C vs A | 30/30 | 0/28 | **+1.00 [+0.83, +1.00]** | 1.0e-16 |
| **monitor_takeout, C vs B** | 30/30 | 29/30 | diff +0.03 [−0.08, +0.17] | **1.00 (ns)** |

Прежние значения (+0.93, +0.90, +0.93 при p_adj 6.8e-14 … 1.9e-13)
получены на загрязнённом `detected` и недействительны.

**Читать так.** Три сравнения из четырёх дают полное разделение:
интервал Newcombe не накрывает ноль и упирается в единицу сверху. При
этом **объявленный нулевой результат остался нулевым** — правка не
вытащила его из ns. Это и есть аргумент, что исправление не подгоняло
результат: оно усилило то, что и так было сильным, и не спасло то, что
было объявлено нулевым.

Нулевой результат объявленного сравнения отчитывать, не прятать.
Всё остальное — exploratory без коррекции, включая `monitor_takeout,
B vs A` (**29/30 против 0/28**, p < 0.001), которая и есть демонстрация
SPOF и которая после правки стала резче.

Воспроизвести:

```bash
python - <<'PY'
import pandas as pd
from metrics.stats import fisher_exact, newcombe_diff_ci, holm
v=pd.read_csv('runs_campaign/campaign_master.csv').query('valid==True')
c=lambda at,a:(int(v[(v.attack==at)&(v.architecture==a)].detected.sum()),
               len(v[(v.attack==at)&(v.architecture==a)]))
fam=[("detector_takeout+gps_spoofing","C",b) for b in "AB"] + \
    [("monitor_takeout+gps_spoofing","C",b) for b in "AB"]
p={}
for at,x,y in fam:
    (k1,n1),(k2,n2)=c(at,x),c(at,y)
    p["%s %s-%s"%(at[:8],x,y)]=fisher_exact(k1,n1-k1,k2,n2-k2)
    print(at[:8],x,y,"%d/%d vs %d/%d"%(k1,n1,k2,n2),
          "%+.2f %s"%(k1/n1-k2/n2, newcombe_diff_ci(k1,n1,k2,n2)))
print(holm(p))
PY
```

### 3.3 Mission degradation, м (медиана)

| атака | A | B | C |
|---|---|---|---|
| detector_takeout+gps | 49.8 | 49.8 | **0.2** |
| monitor_takeout+gps | 49.9 | 49.9 | 19.7 |
| gps_spoofing | 49.8 | 49.8 | 19.7 |
| command_injection | 37.5 | 37.5 | **0.4** |
| comm_disruption | 1.1 | 1.2 | 1.6 |

### 3.4 Координационная целостность (R11)

Фазовое расхождение, м (медиана):

| атака | A | B | C |
|---|---|---|---|
| detector_takeout+gps | 14.1 | 12.4 | **172.9** |
| monitor_takeout+gps | 14.3 | 14.1 | 144.0 |
| gps_spoofing | 14.1 | 15.4 | 145.5 |
| command_injection | 154.1 | 151.9 | **0.2** |
| comm_disruption | 106.5 | 110.6 | 117.1 |

Геометрическое отклонение, м (медиана):

| атака | A | B | C |
|---|---|---|---|
| detector_takeout+gps | 74.7 | 74.2 | **33.1** |
| monitor_takeout+gps | 74.1 | 74.5 | 52.7 |
| gps_spoofing | 67.8 | 71.4 | 52.9 |
| command_injection | 57.0 | 59.2 | **0.1** |
| comm_disruption | 36.2 | 36.9 | 36.3 |

Шумовой пол на чистых прогонах: медиана 0.1 м фазы, 0.0 м геометрии,
IQR до 0.4 м. Хвост тяжёлый: 4 из 88 чистых превышают 10 м фазы, 2 из 88
превышают 30 м геометрии, один достиг 190 м / 232 м без атаки вообще.
**Фаза — надёжный различитель, геометрию подавать с оговоркой про хвост.**

### 3.5 Восстановление — MTTR functional и Recovery Success Rate

Определения из `metrics/derived.py`: MTTR functional = t(рост отклонения
от плана прекратился) − t(isolation_announce). Recovery success =
отклонение перестало расти в пределах окна наблюдения.

MTTR functional, с (медиана):

| атака | A | B | C |
|---|---|---|---|
| detector_takeout+gps | 54.40 | 54.39 | **0.13** |
| monitor_takeout+gps | 53.96 | 51.38 | 16.37 |
| gps_spoofing | 51.00 | 50.92 | 16.36 |
| command_injection | не наступает | не наступает | **0.12** |
| comm_disruption | 0.15 | 0.15 | 0.14 |

Recovery success rate:

| атака | A | B | C |
|---|---|---|---|
| command_injection | **0/15** | **0/15** | **14/15** |
| detector_takeout+gps | 26/28 | 28/30 | 30/30 |
| monitor_takeout+gps | 27/28 | 29/30 | 30/30 |
| gps_spoofing | 12/15 | 15/15 | 15/15 |
| comm_disruption | 15/15 | 14/15 | 12/13 |

**Читать осторожно.** У C рост отклонения прекращается потому, что
архитектура действует. У A и B при спуфе он прекращается потому, что
смещение 50 м насыщается само. Обе величины — «деградация перестала
расти», механизм разный, и это надо сказать прямо, иначе 51 с у A будет
прочитано как «A тоже восстанавливается». При command injection
насыщения в пределах окна нет: подменённый waypoint продолжает уводить
борт до конца наблюдения, поэтому у A и B стабилизация **не наступает в
пределах окна наблюдения (60 с)**.

⚠️ Формулировку «уводит неограниченно» и «не наступает вообще» не
использовать: окно 60 с, за его пределами данных нет. Утверждение о
неограниченном уходе выходит за длительность эксперимента. Правильная
форма — «стабілізація не настала протягом вікна спостереження».

### 3.6 Прочие метрики табл. 3.14

- `Time to Isolation` — медиана **52 мкс**, максимум **548 мкс**, во всех
  архитектурах. ⚠️ Прежнее «40–59 мкс» неверно: «59» было 59 **мс** из
  прогона с предатаковым ложным срабатыванием, прочитанные как мкс. См.
  R19.1; гейт исправлен, число берётся после пересборки.
  Локальное внутрипроцессное решение, архитектуры не различает.
  Отчитать явно как нулевой результат.
- `Containment Success Rate` (бывш. `isolation_success`) — 295/309.
  ⚠️ Метрика измеряет **не работу механизма изоляции**, а то, что ни один
  нецелевой борт не сошёл с маршрута. В текст: «частка прогонів без
  поширення наслідків за межі цільового борту». Глава 3 уже определяет её
  так же. См. R19.2. Различает слабо: A под
  monitor_takeout 24/28, A под comm_disruption 12/15.
- `Residual Mission Functionality` — доля бортов на плане, при трёх
  бортах принимает ровно три значения: 0.33 / 0.67 / 1.00. Указать
  гранулярность. **Обязательно ставить рядом с фазой:** у C при
  detector_takeout это 1.00 при фазовом отставании 173 м.
- `Total Response Time` — только у C, n=91, медиана 7.43 с, диапазон
  0.02–25.39 с. **Два значения отрицательные, см. раздел 8.**
  По клеткам: detector_takeout 30, monitor_takeout 30, command_injection
  15, gps 15, comm_disruption **1**. Медиану comm_disruption по одному
  прогону не подавать как результат.

### 3.7 Стоимость меша (R12)

| арх | опубликовано сообщений | опубликовано байт | доставлено | потеряно |
|---|---|---|---|---|
| A | **0** | 0 | 0 | 0 |
| B | **0** | 0 | 0 | 0 |
| C | 457 ± 23 | 121 735 ± 5 931 | 914 | 0 |

Ноль у A и B точный, по построению (`NoOpMesh`), это измерение, а не
оценка. Состав по всему корпусу: `peer_position` 39 198 сообщений против
486 на всю безопасную сигнализацию (`isolation` 190, `recovery_ack` 148,
`recovery_req` 148). **Почти вся цена меша — периодический маяк
положения, сама координация безопасности почти бесплатна.** Для
обсуждения масштабирования: маяк растёт как O(N²) в полносвязном меше,
0.75 КБ/с — цифра для трёх бортов.

### 3.8 Ложные срабатывания (R13, уточнено R19.3–19.4)

⚠️ **Две разные величины, не смешивать.** Ниже — вторая колонка (все
валидные прогоны, чистое окно). Фоновая частота на ЧИСТЫХ полётах:
**A 2/35, B 4/35, C 0/35.** У C на чистых полётах ложных срабатываний
**нет вообще**; оба её FP-прогона — предатаковые окна атакующих
прогонов, то есть другой класс экспозиции. См. R19.3.

| арх | прогонов с FP | событий | по детекторам | событий на прогон с FP |
|---|---|---|---|---|
| A | 6/136 (4.4 %) | 13 | heartbeat 11, gps 2 | 3, 3, 3, 2, 1, 1 |
| B | 9/140 (6.4 %) | 15 | gps 9, heartbeat 6 | 3, 3, 2, 2, 1, 1, 1, 1, 1 |
| C | 2/138 (1.4 %) | 13 | cross_check 10, gps 3 | **8, 5** |

Fisher: C vs A p=0.172, C vs B p=0.060, A vs B p=0.597.
**Различий в частоте нет. Различие в радиусе.** В A и B ложная тревога
остаётся там, где сработала (1–3 события); в C она расходится по мешу и
становится событием уровня роя (5 и 8). Тот же канал, что разносит
достоверный сигнал, разносит и ложный. Цена — зеркальное отражение
выгоды, а не дополнительная цена.

Цена одного ложного срабатывания, измеренная: `B_none_r4_1785487217`,
**базовый прогон без атаки** — ложная gps-детекция, за ней
`isolation_announce`, здоровый рой разошёлся на 190 м по фазе и 232 м по
геометрии, `residual_mission_func` 0.67.

**Не писать** «меш удваивает фоновый шум» — опровергнуто полным
корпусом, суммарно 13 / 15 / 13 события.

### 3.9 Свип по потерям меша (R10)

Сценарий: C × `detector_takeout+gps_spoofing`, локальные детекторы цели
заглушены, спуф ловит только `cross_check` соседей через меш.
255 валидных прогонов, belief-гейт.

| потери | n | выявлено | доля | Wilson 95 % CI | MTTD медиана, с |
|---|---|---|---|---|---|
| 0.00 | 28 | 27 | 0.964 | [0.82, 0.99] | 7.48 |
| 0.10 | 29 | 29 | 1.000 | [0.88, 1.00] | 7.45 |
| 0.20 | 30 | 28 | 0.933 | [0.79, 0.98] | 7.48 |
| 0.30 | 30 | 19 | 0.633 | [0.46, 0.78] | 7.47 |
| 0.40 | 28 | 16 | 0.571 | [0.39, 0.73] | 7.52 |
| 0.45 | 30 | 14 | 0.467 | [0.30, 0.64] | 7.51 |
| 0.50 | 30 | 14 | 0.467 | [0.30, 0.64] | 7.51 |
| 0.55 | 26 | 12 | 0.462 | [0.29, 0.65] | 8.07 |
| 0.60 | 12 | 4 | 0.333 | [0.14, 0.61] | 7.76 |
| 0.65 | 12 | 3 | 0.250 | [0.09, 0.53] | 8.28 |

Деградация монотонная и плавная, 0.96 → 0.25, плато около 0.46 на
0.45–0.55, **в ноль не обваливается**. A и B ≈ 0 на всех уровнях: без
меша второго канала детекции нет вовсе. MTTD от потерь не зависит.
Указывать n по каждому уровню: на 0.60 и 0.65 их 12 против 26–30.

⚠️ **В `RESULTS_NOTES.md` R10 есть блок про пилотную версию этой
таблицы** (30/30 при потерях 0 и 0/30 при 0.6). Она **недействительна**:
при высоких потерях меш поднимался медленно, GZBridge не успевал
применить `SIM_GPS_OFF_N`, и спуф не садился. Это были прогоны «атаки не
было», а не «не задетектил». Исправлено `--post-launch-settle` и
belief-гейтом.

### 3.10 Разложение MTTD (R9)

MTTD = first_cross + (sustained_samples − 1) / rate = 1.278 + 2.03 ≈ 3.3 с.

**2.0 с из 3.1 — фиксированный пол**, заданный `sustained_samples=3` и
частотой ESTIMATOR_STATUS 1 Гц. Пол одинаков для A, B и C, поэтому в
архитектурном сравнении сокращается. У `cross_check` свой пол от
`peer_publish_period_sec = 1.0`. **3.3 с и 8.1 с не сравнивать как
«быстрее / медленнее»** — они не разложены на одинаковые составляющие.

### 3.11 Чувствительность правила подтверждения (R14, OPEN-3 закрыт)

Пересчитано офлайн по сохранённым рядам `pos_horiz_ratio`, без прогонов,
только сценарий `gps_spoofing` (45 прогонов).

| k | детекция | чистых прогонов с FP |
|---|---|---|
| 1 | 45/45 = 1.00 | **6/105 = 0.057** |
| 2 | 43/45 = 0.96 | 2/105 = 0.019 |
| **3 (в системе)** | **43/45 = 0.96** | **2/105 = 0.019** |
| 4 | 43/45 = 0.96 | 2/105 = 0.019 |
| 5 | 40/45 = 0.89 | 2/105 = 0.019 |
| 6 | 32/45 = 0.71 | 2/105 = 0.019 |

k=3 стоит на плато: k=2, 3 и 4 дают одно и то же. Это сильнейшая
доступная защита выбора параметра. Промах не бесплатно чинится: k=1
возвращает его, но утраивает ложные срабатывания (6/105 против 2/105).

⚠️ **Знаменатель FP исправлен 4 августа.** До этого в таблице стояло
`4/90` и `1/90`. Девяносто — счёт чистых прогонов до добора корпуса; в
валидном корпусе их **105** (35 на каждую архитектуру). Отношение k=1 к
плато при этом не «в четыре раза», а **в три**, при любом выборе окна.
Колонка детекции была верна и не менялась.

**Окно назвать явно.** Детекция считается по пост-атаковому окну
(45 прогонов `gps_spoofing`), ложные срабатывания — по всей серии
(105 чистых прогонов: атаки нет, поэтому пост-атакового окна тоже нет).
По пост-атаковому окну FP дали бы 3/105 и 1/105; выбрана вся серия как
более консервативная оценка фонового шума. Рис. 4.2 несёт эту строку в
подписи.

Проверить одной командой:

```bash
python -c "
import pandas as pd
c=pd.read_csv('runs_campaign/campaign_master.csv').query('valid==True and attack==\"none\"')
for k in range(1,7):
    print(k, int((c.ratio_maxcons_full_fleet>=k).sum()), '/', len(c))"
```

Механизм непрямого срабатывания установлен на конкретном прогоне
`run_A_gps_spoofing_r1_1785561888`: спуф сел (belief 50.29 м), сигнатура
дошла до потолка (peak 2.0), первое пересечение на +0.808 с, но
`n_above = 2` и `max_consecutive = 1`. Два превышения, не подряд, правилу
нужно три. Сигнатура была, правило её отвергло.

**Окно детекции указывать явно:** пост-атаковое окно даёт 43/45, полная
серия 44/45. Разница в один прогон.

### 3.12 Ablation политики восстановления (R15)

Набор `pass1` (60 прогонов) — не брак, а плечо сравнения политик. Старая
разрушительная реакция `restart_process` на потерю heartbeat: **36.65 м**
отклонения от миссии против **1.34 м** у текущей политики удержания.
Глава 4 может показать с данными по обе стороны, что несоответствующая
реакция на атаку доступности стоит дороже, чем отсутствие реакции.

---

## 4. Пять несущих выводов

**Формулировка главного вывода.** Не «C всегда лучше», а:

> **Архітектура C забезпечує додаткову стійкість тоді, коли
> скомпрометовано плоскість безпеки або командний канал, однак характер
> виграшу залежить від того, яка інформація збереглася та яку дію
> реагування можна безпечно виконати.**

Под обычным GPS-спуфингом преимущества по детекции нет: A 14/15,
C 14/15. Под comm_disruption различий тоже нет.

1. **Выигрыш при компрометации плоскости безопасности.** detector_takeout:
   локальный детектор заглушён, A и B слепы (**0/28 и 0/30**), C ловит
   через mesh cross_check (30/30) и сдерживает ущерб (0.2 м против
   потолка 49.8 м).
2. **SPOF централизации.** monitor_takeout: общий домен A рушит все
   мониторы (**0/28**, борт угнан на 49.9 м); B (29/30) и C (30/30)
   выживают. Различие B и C незначимо — объявленный нулевой результат.
3. **Размен — свойство ДЕЙСТВИЯ восстановления, а не self-healing (R11).**
   Внутри C: guard+resume при инъекции даёт фазу 0.2 м и геометрию 0.1 м;
   loiter при спуфе — фазу 172.9 м при геометрии 33.1 м. Принцип:
   эффективность восстановления ограничена тем, уцелела ли информация, на
   которую оно опирается. Спуф разрушает саму оценку положения, лететь
   дальше не по чему.
4. **Цена — не в частоте (R13).** По частоте C не шумнее: на 35 чистых
   полётах у C **0 ложных срабатываний** против 2 у A и 4 у B; Fisher по
   всему корпусу различий не даёт. Различие — в том, что два FP-прогона C
   дали каскады из 8 и 5 событий против максимума 3 у A и B.
   ⚠️ **«Радиус» пока не измерен:** 8 и 5 — это счёт СОБЫТИЙ, а не
   уникальных бортов. Оба прогона C — предатаковые окна атакующих
   прогонов, то есть другой класс экспозиции, чем базовые события A и B.
   До пересчёта говорить «каскад событий», а не blast radius. См. R19.4.
5. **Восстановление после атаки на командный канал — трёхстороннее
   разделение.** command_injection: стабилизация не наступает у A (0/15) и
   B (0/15), наступает у C (14/15) за 0.12 с. Механизм: подменённый
   waypoint уводит борт до конца окна наблюдения, насыщения в его
   пределах нет (о поведении за пределами 60 с данных нет); `guard(sysid)`
   фильтрует команду и миссия возобновляется.

**Вывод 5 новый.** Он получен из уже собранных данных и до этой сессии
нигде не отчитывался — см. раздел 7.

---

## 5. Рисунки

✅ **Набор закрыт: четыре рисунка.** Спецификация и обоснование каждого —
`FIGURE_SPECS.md`. Одна команда пересобирает весь набор с нуля:

```bash
python -m metrics.figures_ch4
```

| рисунок | что отвечает |
|---|---|
| `fig4_1_losssweep` | граница применимости детекции через меш (R10) |
| `fig4_2_sustain` | плато правила подтверждения k (R14) |
| `fig4_3_tracks` | физические последствия двух атак, сетка 2 × 3 |
| `fig4_4_tradeoff` | размен сдерживание × координация (R11) |

**Карта режимов 5 × 3 убрана из набора** после рецензии: она сворачивала
пять свойств в один порядковый балл, класс «відновлено повністю» не
опирался ни на одну меру восстановления, а скобки режимов назначались по
подписи строки. Её содержание подаётся **генерируемой** таблицей:

```bash
python -m metrics.plots_regime --table
```

**Что рисунки НЕ утверждают** — держать под рукой при написании:

- 4.1 показывает только C/cross_check; A и B в этом свипе не оценивались
- 4.2 — чувствительность локального GPS-детектора, а не Detection Rate
  архитектуры
- 4.3 — шесть непарных прогонов без общего seed: иллюстрация механизма,
  не доказательство причинности; MTTD между рядами не сопоставляется
- 4.4 — действие восстановления не варьировалось независимо от атаки,
  поэтому отделить вклад действия от вклада сценария нельзя

**Ничего не потеряется, даже если удалить всю папку `figures/`.**

**Ничего не потеряется, даже если удалить всю папку `figures/`.**
Рисунки это слой отображения, а не слой хранения: всё пересобирается из
`campaign_master.csv` и `runs_final/detection_vs_loss.csv` командой
`python -m metrics.plots`.

**Не покрыто рисунком осознанно:** стоимость меша (у A и B точные нули,
это строка таблицы), разложение MTTD (R9, четыре строки), ablation
политики восстановления (R15, два числа).

---

## 6. Реестр раскрытия

| пункт | в текст? | причина |
|---|---|---|
| Валидационный гейт и 1 исключённый прогон | **да**, как метод | влияет на знаменатель; 1 из 435 сам по себе отвечает на возражение о post-hoc правиле |
| Поток выбраковки, N по клеткам, упавшие прогоны | **да**, как метод | читатель должен уметь восстановить выборку |
| Провенанс: дофиксовые батчи исключены | **да**, одна строка | без слова «дефект» |
| Зависшая кампания → чанковый харнесс | **да**, как методология | обосновывает протокол прогона; 150/150 чисто после |
| Кривая чувствительности правила подтверждения (R14) | **да** | sensitivity analysis; её отсутствие было бы слабостью |
| Определение окна детекции (пост-атаковое или полное) | **да**, одно предложение | это определение, а не признание |
| Фон ложных срабатываний с атрибуцией по детекторам | **да** | отделяет компонент от архитектуры |
| Ненадёжность belief под monitor_takeout → выбор ground truth | **да**, как метод | обоснование источника истины |
| `residual_mission_func` 1.00 рядом с фазой 173 м | **да**, с примирением | читатель выведет противоречие из опубликованных чисел |
| Пилотная версия таблицы R10 | **да**, одной строкой в методологии | объясняет, почему свип гейтуется по belief |
| Legacy `coordination_loss`, дублировавшая residual | **нет** | черновая метрика, не отчитывается |
| Дефект учёта окна FP (R13) | **нет** | найден и исправлен до любого отчитанного числа |
| Дефект якоря `impact_scope` (R5) | **нет** | затронул данные, которые не отчитываются |

Тон важнее содержания: sensitivity analysis, поток выбраковки и
характеризация FP — стандартные разделы сильной эмпирической работы.
Писать «мы измерили X и он ведёт себя так», никогда — «на жаль, наш
детектор іноді не спрацьовує».

---

## 7. Что терялось между хендоффами

Зафиксировано, чтобы не потерялось снова.

**7.1. Свип по потерям.** Правильные числа были получены и записаны в
`HANDOFF_NEXT.md` (255 прогонов, 0.96 → 0.25, плато 0.46, в ноль не
обваливается), но в `RESULTS_NOTES.md` осталась пилотная таблица с
«0/30 при 0.6», и именно на неё ссылались следующие хендоффы.
**Восстановлено**, таблица переписана, добавлен `fig8_losssweep`.

**7.2. MTTR functional и Recovery Success Rate.** Метрики посчитаны по
всей кампании (`metrics/derived.py`, коммит `d6acfb6`) и **ни разу не
анализировались**. Поиск по всем хендоффам, `PROJECT_STATE.md` и
`RESULTS_NOTES.md` не даёт ни одного упоминания `mttr_functional_s` или
`degradation_stopped`. Все разговоры про MTTR относились к старой
live-метрике: 8.07 с холодного старта PX4, затем 15 мс латентности
диспетча. Из-за этого `HANDOFF_CH3_ALIGNMENT.md` предписывал отчитать
MTTR нулём как выродившуюся. **Это указание неверно.**
Восстановлено, добавлен `fig9_recovery`.

**7.3. Ground truth.** Решение «эталон только Gazebo, оценка PX4
отравлена спуфом» зафиксировано в `HANDOFF_ANALYSIS.md`, но в текст
диссертации не попадало до этой сессии. Внесено в 3.5.4.

**7.4. Утверждение про `cross_check`.** В `HANDOFF_WRITING.md` стояло
«`cross_check` по построению доверяет соседям». Неверно: детектор
проверяет заявление соседа против кинематики. Из-за этой формулировки
одна сессия целиком ушла на проектирование ненужного византийского
эксперимента. **Исправлено** в обоих файлах, точная граница описана в
разделе 9.

**7.5. Два раздела с меткой R9.** Разложение MTTD и свип по потерям
носили один номер. Свип переименован в R10.

**7.6. Числа R11–R13 расходились с мастер-файлом** в строках
`gps_spoofing` и `comm_disruption`, в знаменателе FP у A и в стоимости
меша. Приведены к мастер-файлу.

---

## 8. Открытые пункты

**8.1. `total_response_time_s`: два отрицательных значения из 91.**
`C_gps_spoofing_r4_1785309955` (−31.5 с) и
`C_command_injection_r1_1785410951` (−23.6 с). Это ровно те два прогона
C с фронтовыми ложными срабатываниями из R13 (8 и 5 событий). Ложное
срабатывание до инъекции запустило изоляцию и восстановление, а метрика
считается от `inject_start`, отсюда минус. **Решить до главы 4:** либо
гейтовать метрику по тому же пост-атаковому правилу, что и детекцию,
либо не отчитывать её вовсе. Без этих двух значений метрика чистая.

**8.2. Пять текстовых правок главы 3, не блокирующих главы 4–5.**
Полные тексты в `thesis_text/INSERTS_CH3_ROUND2.md`, части B-1…B-5:

- B-1: табл. 3.11, развести distributed security context sharing
  (реализовано, несёт главный результат) и coordinator election
  (упрощён). Сейчас первое ошибочно отнесено к упрощённому.
- B-2: 3.5.6, добавить границу по аутентификации транспорта.
- B-3: 3.1.2.3, снять непроверенное утверждение о том, что влияние
  скомпрометированных узлов «може бути знижений шляхом ізоляції».
- B-4: 3.4.4, предложение о том, что эмпирические аналоги временных
  характеристик ограничены снизу параметрами измерительного контура.
- B-5: 3.2.3, отнести перераспределение ролей и маршрутизацию к целевой
  модели, а не к PoC.

**8.3. Состояние диссертации.** Написаны главы 2 и 3. **Нет вступа**
(мета, об'єкт, предмет, задачі, наукова новизна, положення на захист),
**нет глав 4 и 5**, нет общих выводов. Глава 2 — 4 620 слов и 26
источников; в 2.2 объявлен systematic literature review, но нет ни
одного числа отбора: сколько найдено, сколько отсеяно, сколько вошло в
корпус. Это отдельная задача, от результатов не зависит, делается
параллельно.

**8.4. Три комментария Адамова** в черновике не закрыты. Два из трёх
(«ссылка на загрузку», «референсы на воспроизводимый setup, платформа
open source?») закрываются ссылкой на репозиторий.

**8.5. Рабочие пометки** «Gap link: … твій внесок» идут в компиляцию и
видны в PDF на последних страницах. Убрать перед сдачей.

**8.6. Публикации** в материалах не фигурируют — требование с длинным
лагом, проверить статус.

---

## 9. Ограничения (в текст как ограничения, НЕ как задачи)

Симуляция без RF и железа. Три борта. Меш поверх ZeroMQ на localhost
(частично закрыто свипом по потерям, R10). Простой сменный детектор.

**Византийский случай отсутствует.** Формулировать точно:

`CrossCheckDetector` — кинематическая проверка достоверности.
Haversine-дистанция между двумя последовательными объявлениями пира
делится на Δt и сверяется с `max_velocity_mps` (25 м/с при крейсере ~12)
плюс `position_error_margin_m` (10 м). Заявление соседа проверяется
против ограничения, которым заявитель не управляет, против физики.
Требование 3.2.4 реализацией по существу выполнено.

Остаточная щель узкая и состоит из двух частей:

1. Византиец, врущий **правдоподобно** — фабрикующий гладкую физически
   возможную ложную траекторию — проверку пройдёт. Но проверка попирная,
   поэтому оболгать он может только себя: событие выставляется на
   `ann.uav_id`, то есть на самого объявителя.
2. Подставить **здоровый** борт он может, потому что `uav_id` лежит в
   payload, а `core/mesh.py` (ZeroMQ PUB/SUB, topic + payload) не
   связывает его с сокетом отправителя. Это аутентификация транспорта, а
   не архитектура безопасности.

Подавать как объявленную границу scope со ссылками на существующие
решения (BFT-консенсус, репутационные механизмы, блокчейн-шардинг, DAG),
не как извинение. Размеченный датасет на 435 прогонов для будущей работы
уже собран. Источники: SwarmRaft (arxiv 2508.00622), Local
Conformity-Based Evolutionary Game под византийской атакой
(arxiv 2606.21206), blockchain sharding для роёв
(Sci Reports s41598-025-20359-1).

---

## 10. Решения, которые НЕ переобсуждать

- Различия архитектур — только конфигом
  (`configs/architecture_{a,b,c}.yaml`). Ветвления `if architecture` в
  domain-коде нет и не будет.
- Детектор и действие восстановления — сменные компоненты, вне вклада.
- ML-детектор не добавляем: архитектурно ничего не меняет, требует
  перепрогона всей кампании и переносит сравнение на чужое поле. В
  будущие исследования — да, с указанием, что размеченный датасет на 435
  прогонов уже собран.
- Свип по магнитуде спуфа выкинут: характеризует детектор, не вклад.
- Ground truth — только Gazebo. Оценка PX4 отравлена спуфом.
- Старая `coordination_loss` (дубликат `residual_mission_func`) не
  публикуется; в мастер-файле лежит как `legacy_*` для прослеживаемости.
- Прогонов не будет. Ни больших, ни малых.

---

## 11. Устаревшее — НЕ брать из ранних сообщений

- «Меш удваивает фоновый шум» — опровергнуто полным корпусом.
- FP-числа 10/9/13 — до исправления учёта baseline. Верные: 13/15/13
  события, 6/9/2 прогона.
- «254 валидных прогона» — до добора. Верно: **414**.
- `runs_sweep/` не существует, артефакты R10 в `runs_final/`.
- MTTD `none` в старых таблицах — это ложные срабатывания, а не детекция.
- «MTTR 15 мс» и «MTTR 8.07 с» — старая live-метрика, не та, что в
  мастер-файле. См. 3.5.
- Таблица R10 с «0/30 при потерях 0.6» — пилот, см. 3.9.

---

## 12. Первый шаг в новом чате

1. Подтвердить `git log --oneline -1` и `python -m pytest -q` (816).
2. Прогнать проверку провенанса из раздела 2, убедиться в 435 / 414 / 20 / 1.
3. Решить пункт 8.1 (отрицательные `total_response_time_s`).
4. Писать **раздел 4.1**: конфигурация кампании, поток выбраковки
   (657 на диске → 435 в анализе → 414 валидных), критерии валидности,
   провенанс, протокол прогона чанками.

Числа — только из мастер-файла.
